pre-commit:
  parallel: true
  jobs:
    - name: go-fmt
      glob: "*.go"
      run: go fmt {staged_files}
      stage_fixed: true

pre-push:
  parallel: true
  jobs:
    - name: go-test
      run: go test -v ./...
